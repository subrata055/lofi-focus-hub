version: '3.8'

services:
  lofi-focus-hub:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/focus_sessions.db
    restart: unless-stopped