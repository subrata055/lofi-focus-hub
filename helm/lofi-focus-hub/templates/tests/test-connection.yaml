apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lofi-focus-hub.fullname" . }}-test-connection"
  labels:
    {{- include "lofi-focus-hub.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lofi-focus-hub.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
